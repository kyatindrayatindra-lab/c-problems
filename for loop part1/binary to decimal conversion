#include <stdio.h>

int main() {
    int n, decimal = 0, base = 1;
    int digit;

    scanf("%d", &n);

    for (; n > 0; n /= 10) {
        digit = n % 10;
        decimal += digit * base;
        base *= 2;
    }

    printf("%d", decimal);

    return 0;
}
